import CartClient from './CartClient'

export const metadata = {
  title: 'Shopping Cart',
  description: 'View and manage items in your organic shopping cart.',
}

export default function CartPage() {
  return <CartClient />
}
